apiVersion: v1
kind: Service
metadata:
  name: {{ include "authsys-db-chart.fullname" . }}
  labels:
    {{- include "authsys-db-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "authsys-db-chart.selectorLabels" . | nindent 4 }}
