apiVersion: v1
kind: Service
metadata:
  name: {{ include "authsys-backend-chart.fullname" . }}
  labels:
    {{- include "authsys-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "authsys-backend-chart.selectorLabels" . | nindent 4 }}
