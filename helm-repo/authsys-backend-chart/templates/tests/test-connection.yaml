apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authsys-backend-chart.fullname" . }}-test-connection"
  labels:
    {{- include "authsys-backend-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authsys-backend-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
